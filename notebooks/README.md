# Notebooks

Exploration only. **Nothing on the reproduction path may live in a notebook.**

Work in your own subfolder to avoid conflicts. When a notebook produces something the
pipeline needs, port it into `src/` with tests — then the notebook becomes a record of
how you found it, not a dependency.

Clear outputs before committing; notebook diffs are otherwise unreviewable.
